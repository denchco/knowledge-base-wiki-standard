---
version: "1.0"
name: "{{PROJECT_TITLE}}"
description: "Agent-readable visual contract for the subject Wiki."
colors:
  primary: "{{PRIMARY_COLOR}}"
  accent: "{{ACCENT_COLOR}}"
  background: "#f6f8f9"
  text: "#1f2933"
typography:
  body:
    fontFamily: "system-ui"
    fontSize: "1rem"
    lineHeight: "1.6"
rounded:
  component: ".35rem"
---

# Design Contract

Use the pinned DenchCo visual adapter as the baseline, then record only deliberate target-specific token and interaction decisions here. Navigation and synthesis must follow the target's audience and governing question.
