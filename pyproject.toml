[project]
name = "denchco-knowledge-base-wiki-standard"
version = "0.1.0"
description = "Canonical OKF-compatible standard for evidence-governed human and AI wikis."
requires-python = ">=3.12"
dependencies = [
  "graphifyy==0.8.35",
  "zensical==0.0.51",
]

[tool.uv]
package = false
