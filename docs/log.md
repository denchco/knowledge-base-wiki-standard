---
type: Update Log
title: Research and change log
description: Chronological standard history.
status: draft
---

# Research and change log

## 2026-09-09

- **Native appearance proposal**: Generalise a consumer light/dark appearance request into the existing `DKBWS-DESIGN-001` reference adapter. Zensical owns the palette control and saved preference; the action appears immediately before Wide and remains available on mobile. The Standard keeps its light initial appearance and teal colour family.
- **Complete visual boundary**: Name both colour families, recompute inherited renderer/control aliases, and carry the native scheme into Mermaid and same-origin graph views. Source-level diagram structure, graph data and evidence authority are unchanged. Browser verification covers both appearances, keyboard operation, persistence, search/diagram/graph readability and responsive containment. This entry describes an implementation proposal, not accepted consumer adoption or an immutable release.
- **Appearance review correction**: Repair unchecked native radios after Zensical restores a saved scheme. Mirror only the resolved scheme into checked state; retain the native preference, events and keyboard behaviour. Regression checks inspect the first load and restored light/dark states before interaction, then prove keyboard entry, visible focus and switching after reload and navigation at desktop and mobile widths.

## 2026-08-09

- **Workflow and hardening prerelease**: Published the accumulated consumer-derived requirements, exact supported toolchain, identity-verified managed preview, live-Wiki development-response navigation, tracked proposal intake, absolute JJ turn boundary, and remote repository protections as immutable `v0.1.0-rc.4` from exact required-Verify merge commit `4aae4d4efbcea526f28ec8bcbfaf203dae34bc25`. The accepted historical `DKBWS-LINK-002` bridge is now mapped to that release in this strictly later post-release ledger rather than fabricated inside the release commit.
- **Remote enforcement boundary**: Activated a no-bypass `main` ruleset that requires pull requests, resolved review threads, merge commits, and the exact GitHub Actions `verify` check while blocking deletion and non-fast-forward updates. Restricted Actions to full-SHA GitHub-owned actions plus `astral-sh/setup-uv`, retained read-only workflow-token defaults, and enabled secret scanning, push protection, non-provider patterns, validity checks, Dependabot security updates, private reporting, and immutable releases. Release work reads these settings back rather than treating repository prose as remote evidence.
- **Cross-platform diagram fit**: The protected pull-request check exposed wider Linux Chromium text metrics than the local macOS run. Compact upstream labels alone left both diagrams at the same 71px overflow because their shared Human, Agent, and Graph sibling rank governed the width. An exact per-diagram 40px node spacing, reduced from Mermaid's 50px default, preserves those full product labels while giving the Linux renderer a projected 51px overflow. Accessible descriptions, prose, topology, font size, node styling, centring, and the 60px limit remain unchanged; failure output includes the measured overflow for future diagnosis.
- **Managed-preview evidence closure**: Strengthened `DKBWS-RUNTIME-002` from partial ownership/status checks to exact comparison of all twelve governed registry fields, byte-identical generated/installed LaunchAgent content, loaded-job presence, marker identity, and canonical URL equality. Added a fail-closed `service:preview` browser handoff and made live `service:status` a maintainer-only verification gate; distribution/CI now reports the persistent service requirement and capability as not checked.
- **Response-link rendering closure**: Extended `DKBWS-PROMPT-002` detection across Markdown, entity-decoded HTML `a`/`area` links, and plain GFM-autolinked HTTP(S) text, with code/comment/image controls. `--managed-live` now verifies exact service identity, the registered base URL, and HTTP 200 for every displayed Wiki route. Captured violations fail, while `STD-VAL-007` continues to record the host pre-send boundary for uncaptured output.
- **Proposal state and release closure**: Added direct-state and terminal-transition validation, exact issue identity, exact registry bytes at the full recorded revision, strict release-to-registry ancestry, public exact-tag/non-draft/immutable release checks, and stable state/revision/ancestry/release/bridge diagnostics. Restricted pre-registry intake to the one immutable UK Digital Health `DKBWS-LINK-002` bridge; new work must use governed issue intake.
- **Supported Node boundary**: Replaced the permissive `>=24` engine with exact Node `24.19.0` across `.node-version`, the package engine, npm `engine-strict`, executable runtime validation, the starter adaptation map, the documentation snapshot, and CI. This prevents a numerically newer short-lived Node line from being treated as qualified merely because it satisfies a minimum range.
- **Patched adapter selection**: Locked Mermaid `11.16.1` with DOMPurify `3.4.13`, Zensical `0.0.53`, and Graphify `0.9.37`, while retaining vis-network `10.1.0`. Raised the npm audit boundary from high to moderate so the previously tolerated Mermaid and DOMPurify findings now fail verification.
- **Executable qualification evidence**: Activated Node `24.19.0`, reconstructed the npm and Python environments from their locks, and passed exact-version enforcement, moderate-severity npm audit, Python audit, Graphify regeneration/publication, strict Zensical build, and the desktop/mobile browser contract. Removed `STD-VAL-003` because its deliberate adapter-upgrade trigger is satisfied; a later exact version pair remains a new qualification decision.

## 2026-08-08

- **Jujutsu 0.44 qualification**: Replaced the exact Standard Production maintainer pin `0.39.0` with `0.44.0` after auditing every upstream breaking change from 0.40 through 0.44 against the repository's actual command and configuration surface. None affected the governed root, Git-root, raw Git-SHA, log-template, status, commit, or colocated-initialization operations.
- **Executable compatibility evidence**: Verified the official 0.44.0 arm64 binary in a disposable colocated copy before changing the shared installation, then required the installed 0.44.0 binary to pass maintainer provenance and the complete repository verification contract. Removed `STD-VAL-004` because the intentional qualification trigger was satisfied; later pre-1.0 versions remain separate exact qualification decisions.

## 2026-08-07

- **Live Wiki development-response navigation**: Added `DKBWS-PROMPT-002` after auditing the prompt, requirement catalogue, profile, and shared instructions. The previous bootstrap explicitly allowed both canonical file links and Wiki URLs, while runtime identity rules governed browser handoff rather than development-response hyperlinks; no stable requirement, exact capability, or response fixture closed that gap.
- **Executable response-link contract**: Declared `development_response_links: live-wiki`, made the configured canonical Human Wiki origin mandatory for that capability, synchronized root/starter and Codex/Claude instruction surfaces, and added `npm run response:links:check -- --base-url <canonical-url>`. Captured drafts now reject file/editor URLs, clickable local or repository-relative paths, and live Wiki URLs outside the configured origin.
- **Migration and enforcement boundary**: Development handoffs replace file-based Wiki navigation with exact live routes only after service-identity and route checks. Plain repository paths remain available as non-clickable implementation-artifact identifiers, and an unavailable route is disclosed. Repository verification can make captured violations nonconforming, but absolute interception of uncaptured model output remains dependent on a host-provided pre-send hook.
- **Durable Standard proposal intake**: Added `DKBWS-UPDATE-002` and recorded its executable/normative implementation at Git commit `b08f19a9f8fd07aece348d23751791adedcfae53` (JJ change `wzrknyquvruuwtuslslvuspsqqmpxrlt`). A consumer proposal is now a tracked, schema-validated record whose full Git implementation revision is primary provenance and whose JJ change ID is optional navigation metadata.
- **Independent lifecycle facts**: Separated local verification, deterministic preparation, payload-and-form approval, user submission observations, later exact issue linkage, upstream classification/decision, immutable release inclusion, and optional consumer adoption. A closed issue remains only an observed issue state; it never implies acceptance, release, or adoption.
- **Historical bridge and public boundary**: Registered the verified reader-navigation proposal as reusable, accepted `DKBWS-LINK-002`, and pending release through pre-registry local history, without fabricating an issue or release. The local candidate retains GET-only reconciliation and fail-closed form/label checks; publication, label creation, issue submission, release creation, and consumer pin changes remain separate authority decisions.
- **Canonical governing-question consistency**: Promoted the latest independently verified Health IT Governance consumer follow-up as conditional `DKBWS-HUMAN-004`. A seeded Wiki maps one canonical question source and a bounded reader-source set; every exact reader-facing repetition uses the governed callout, while paraphrases, ordinary quotations, historical/source records outside the set, and subject-empty Wikis stay neutral or not applicable.
- **Explicit incorporation boundary**: Strengthened selected-profile reporting so every inherited requirement has an explicit result. Normative outcomes and applicability accounting are absolute; project question text, health-specific routes, corpus counts, regex/code choices, and equivalent adapter implementations are deliberately not copied into the portable Standard.
- **Reference proof and migration**: Added manifest mappings, a stable source checker, positive/negative/subject-empty fixtures, starter guidance, and desktop/mobile Chromium checks across every discovered repetition route. Existing seeded Wikis inventory exact repetitions and migrate them to the governed marker; subject-empty Wikis invent no question.
- **Managed-preview classification**: Classified the verified consumer collision pattern as core rather than project-specific: an HTTP 200 from an occupied canonical port can identify the wrong Wiki, so listener presence and generic HTTP health are insufficient.
- **Normative runtime split**: Retained `DKBWS-RUNTIME-001` as the baseline canonical-URL HTTP check and added `DKBWS-RUNTIME-002` for persistent managed previews. The new requirement binds a stable service ID, project root, endpoint, adapter, and health path in a serialized, atomically published shared-register transaction; rejects registered and unmanaged-listener collisions without killing another process; and requires an exact versioned marker.
- **Reference implementation**: Hardened the macOS LaunchAgent wrapper, added bounded exclusive locking with conservative stale-owner recovery and same-directory atomic replacement, added `service-identity-v1`, static marker and concurrency/wrong-service/live-listener/composite-status fixtures, required the `local_service` capability in Standard Production, and mapped the fixture evidence into full-profile verification.
- **Instantiation and migration**: Updated the URL-only prompt, starter, lifecycle planner, dependency/security boundaries, and browser handoff. A persistent consumer now inventories first, moves only its own service configuration on conflict, updates canonical URL references together, starts the selected adapter, and opens the registered URL only after identity-aware status passes. Port-zero verification remains separate baseline evidence.

## 2026-08-05

- **Reader evidence navigation**: Promoted the independently verified Health IT Governance consumer pattern as conditional Human Wiki requirement `DKBWS-LINK-002`. Every displayed registered identity now links independently to its exact internal source-register row; lists retain each identity, compact ranges retain their displayed endpoints, and named official-publication links use registered source URLs.
- **Reference implementation**: Added stable source-row anchors, an optional idempotent migration, stable lint diagnostics, positive/negative filesystem fixtures, visible citation-bracket styling, and a desktop/mobile Chromium journey that proves focus, accessible identity, exact fragment, destination row, and containment. Subject-empty and citation-free Wikis remain not applicable.
- **Parser hardening**: Corrected the originating helper's Unicode-index, long-fence, reference-definition, HTML, image, and Markdown-destination edge cases before adopting it into the Standard.
- **Development-turn provenance boundary**: Strengthened `DKBWS-PROV-001` from reviewable validated phases to an explicit JJ commit at the end of every file-changing development turn. Verification and Git/Jujutsu inspection run first; failed or unrun checks are disclosed in the local commit and final response, no subjective significance threshold applies, and no-change turns create no empty commit.
- **Prospective migration**: Synchronized the root and starter instructions, Codex/Claude skills, maintenance guidance, dependency contract, initialization prompt, lifecycle stage, conformance aggregation, and executable helper fixture. Existing JJ history remains intact and the rule applies from the upgraded revision forward.
- **Independent product surfaces**: Restored the `v0.1.0-rc.3` Human, Agent, and Graph fan-out under renderer-default Mermaid treatment. The three separately presented surfaces derive from one shared canonical corpus; the homepage paths reconverge on verification, and none of the product boxes gains evidence authority.
- **Bounded topology contract**: Expanded only the three-product reference topology to seven nodes and eight visible relationships, retained the five-node/five-relationship limit elsewhere, and added positive separate-node plus negative combined-node fixtures with responsive fit and collision verification. Fitting surfaces use safe grid centring; the native three-column rank retains governed centred pane containment on the 390px reference viewport without page overflow.

## 2026-08-04

- **Draft navigation promotion**: Promoted the user-directed Health IT Governance consumer pattern as conditional Human Wiki requirement `DKBWS-HUMAN-003`: draft pages use a dedicated edit marker, readable status text, and the same trailing centreline as nested-navigation chevrons.
- **Reference asset and fallback**: Registered SVG Repo's CC0 Pen Circle asset for Zensical, retained equivalent open-licensed markers as the cross-renderer fallback, and made omission explicitly conforming where primary navigation does not expose draft status.
- **Consumer-to-standard route**: Added a proposal-first GitHub issue form for user-authored consumer improvements, with originating revision, reuse classification, evidence, fixture, accessibility, fallback, and migration fields. Local agents may prepare a proposal but require explicit authority before submitting it remotely.
- **Second-consumer reconciliation**: Audited Health IT Governance Beyond the NHS at immutable revision `c96d235c18094243b6493d8aa1c74a38d65eb947`, promoted only reusable/core or preferred implementation patterns, and retained its health-domain content as project-specific.
- **Revision-aware lifecycle**: `DKBWS-UPDATE-001` now resolves same-source commits independently of version labels, loads pinned profiles/catalogues from those commits, exposes introduced requirements, blocks missing/unresolved/diverged pins, and hashes candidate inputs from the exact reported revision.
- **Path-safe provenance**: Fixed provenance and documentation-sync command normalization so a filesystem-significant trailing-space repository root survives Git/Jujutsu discovery; added final-component Standard and documentation fixtures.
- **Consumer verification boundary**: Reserved receipt/report paths now reject project-only summaries, historical lifecycle CI fetches full history, and starter adaptation excludes Standard-maintainer documentation sync unless independently declared.
- **Starter closure**: Added consumer-owned licensing, complete security/source controls, governed graph mount templates, fail-closed required graph verification, independent browser routes, mobile table evidence, and exact-payload proposal safeguards.
- **Mermaid native-default reset**: Classified the user-directed Health IT Governance consumer comparison as a preferred `DKBWS-DESIGN-001` default. The reference entry and architecture diagrams now omit diagram-level initialization and semantic class styling, while the optional role grammar remains compatible for exceptional material distinctions. Accessible descriptions, topology limits, collision checks, and desktop/mobile browser evidence remain required.
- **Mermaid legibility**: Replaced compressed cluster-and-fan diagrams with short top-to-bottom flows, shorter labels, native `basis` connectors, consistent rectangular nodes with `10px` internal padding, ordinary arrows, and prose-held detail. Added bounded topology and rendered collision checks so connectors cannot pass through labels or overlapping nodes while entry and architecture diagrams remain within the content rail.
- **Beginner entry point**: Recast the README around the single repository-address invocation, made the adaptive questions and automatic defaults scannable, moved maintainer mechanics behind progressive disclosure, and added the governed DENCH | CO wordmark plus four trust-oriented badges.
- **URL-only adaptive bootstrap**: Replaced pre-filled topic/profile/URL/deployment fields with a repository-URL-only agent entry point. The first question now selects a research topic seed or subject-empty local initialization; subsequent questions depend on inspected evidence, with exact accent confirmation and automatic local defaults.
- **Subject-empty semantics**: Blank initialization now preserves an explicit awaiting-seed state and omits the governing-question callout rather than inventing subject claims. External research extent and sensitive-source handling are conditional questions; deployment remains none unless separately authorised.
- **Automatic bootstrap revision**: Removed the release tag or commit from the user-facing instantiation prompt. Implementations now resolve the standard repository's current default-branch HEAD once, use its exact commit SHA for the run, and record that immutable SHA in the consumer manifest.
- **Governing-question promotion**: Promoted the mature NHS Data Sharing semantic callout pattern from reusable local precedent to candidate-core Human Wiki requirement `DKBWS-HUMAN-002`; the Standard, Documentation dogfood site, and subject-empty starter now distinguish governed questions from neutral quotations.
- **Rendered verification**: Required the inline-start rail and all question text to equal the runtime-resolved active accent at desktop and mobile widths, with a negative mismatched-colour fixture and a neutral ordinary-quotation control.
- **Migration impact**: Existing Human Wikis only migrate when they already present a governing-question callout: add the semantic marker, bind both colours to the active accent family, and capture built-output browser evidence. Wikis without such a callout record the rule as not applicable.
- **Instruction prerelease**: Published the shared completion-first Codex/Claude operating contract, subject-empty consumer relay, zero-to-three handoff eligibility rule, one-pass completion conditions, and structured parked-work reopen fields as immutable `v0.1.0-rc.2`.
- **Migration boundary**: Consumers moving from `v0.1.0-rc.1` update agent instructions and project-status separation only; the manifest and OKF content model do not change.
- **Release verification**: Required complete local verification, exact-commit GitHub Actions success, deterministic Documentation snapshot synchronization, and the private-source/public-Pages deployment gate for the same pinned Standard commit.
- **Semantic-and-publication prerelease**: Published governed question styling, semantic responsive Mermaid role grammar, corrected authority topology, and deterministic local-versus-public publication scoping as immutable `v0.1.0-rc.3`.
- **Public projection boundary**: Project-specific adoption recommendations and their raw JSON records remain inspectable in the repository and local Wiki, while generated public Pages, search, and Graphify outputs omit them with executable negative checks.

## 2026-08-03

- **Creation**: Established the DenchCo Knowledge Base Wiki Standard candidate repository.
- **Decision**: Adopted OKF v0.2 as the canonical portable foundation rather than only an export sidecar.
- **Decision**: Retained the stronger local evidence-authority hierarchy as an OKF-compatible extension.
- **Decision**: Defined coordinated Human and LLM Wiki products over shared canonical knowledge.
- **Decision**: Selected Zensical and the DenchCo governed visual profile as the first-class human reference renderer.
- **Decision**: Included Graphify, Jujutsu, local service, conformance, prompt/question, release, and future MCP contracts.
- **Verification**: Canonical checks and strict Zensical build passed; dependency audits passed with zero known vulnerabilities after qualifying Playwright `1.62.1`.
- **Runtime**: Reserved and verified the restart-resilient local service at `http://127.0.0.1:8017/` with HTTP 200 health.
- **OKF implementation**: Added v0.2 hard validation, separate strict guidance, manifest validation, lossless export, stable diagnostics, and positive/negative fixtures.
- **Lifecycle implementation**: Added read-only inspection/diff and planning-only initialization/upgrade commands with collision preservation, content hashes, and no apply mode.
- **Production implementation**: Added local Mermaid and graph runtimes, six-layer Graphify enrichment, schema-versioned shared 2D/3D publication, strict Zensical build, governed visual checks, and built-output Playwright verification.
- **Dogfood**: Audited NHS Data Sharing, Regulation 28, InterSystems, X-Lab, and Palantir without modifying them; separated formal claims from inferred adoption evidence and reconciled eleven candidate defects.
- **Governance**: Added security, source handling, contribution, and governance policies; selected one MIT License for original code and accompanying documentation, copyright Andrew Dench; GitHub publication remains undecided.
- **Provenance implementation**: Added stable `DKBWS-PROV-001`, a read-only Git/Jujutsu colocation and current-change check, five-project inferred evidence, and an explicit Git-only distribution mode that cannot masquerade as maintainer provenance. Locally qualified Git `2.50.1` and Jujutsu `0.39.0`; recorded upstream `0.42.0` as available but not yet qualified.
- **Verification requalification**: Qualified the exact Node stack (`@google/design.md` `0.4.0`, Playwright `1.62.1`, 3d-force-graph `1.80.0`, Ajv `8.20.0`, Mermaid `11.16.0`, vis-network `10.1.0`, and YAML `2.9.0`) on the Node 24 LTS CI contract; compiled every Draft 2020-12 schema, schema-validated generated receipts and graph artifacts, added npm/Python vulnerability gates, and made full verification fail any canonical-file mutation.
- **Distribution boundary**: Kept the standard repository as sole normative authority, routed human guidance to the separate documentation Pages site, and added a subject-empty starter contract that forbids root copying, pins an immutable revision, and leaves topic content, URL, and deployment with each consumer.
- **Repository decision**: Selected public `denchco/knowledge-base-wiki-standard` and private-source/public-Pages `denchco/knowledge-base-wiki-documentation`, with MIT copyright Andrew Dench and GitHub private vulnerability reporting.
- **Synchronization implementation**: Added a deterministic, allowlisted, hash-checked standard snapshot that writes only to the documentation repository's isolated snapshot root, refuses local conflicts, and records clean or dirty standard provenance for local development and release gates.
- **Graph publication security**: Replaced raw inline JSON with script-safe serialization for Graphify labels and metadata; a hostile closing-script fixture now guards the boundary.
- **Instruction research**: Audited all immediate child repositories for anti-rabbit-hole handoff controls. DMedC supplied the strongest goal/cycle selection frame, InterSystems the strongest rejection filter, READ-SNOMED the strongest parked/reopen state, and EPMA the clearest concise scope boundary.
- **Instruction decision**: Replaced unconditional exactly-three response boilerplate with completion-first, zero-to-three recommendations. Eligible items must map to unfinished requested scope, a failed or unrun check, or a decision requiring authority; optional adjacent work and filler are forbidden.
- **Agent interoperability**: Added a root Claude `@AGENTS.md` relay, byte-identical Codex/Claude Standard skills, subject-empty starter relay and project-status template, and machine checks for import, parity, policy synchronization, and starter planning.
- **Status reconciliation**: Removed completed repository, documentation, Pages, and `v0.1.0-rc.1` publication work from the live roadmap and validation queue; retained three bounded priorities and structured parked-work reopen conditions.


## 2026-09-08 — rc.5 qualification

Implemented the requested security/runtime repairs, canonical OKF timestamp/source alignment, exact reference toolchain, dependency update controls, optional trusted Codex Stop/plugin adapter and llms.txt v2 publication. Preserved immutable rc.4 history. Runtime inspection distinguishes inspected Mermaid inputs from opaque graph prebundles; host continuation support does not prove absolute pre-send interception. The maintained release/verification records carry the resulting qualification and exact publication identity.
